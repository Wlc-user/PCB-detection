# 无人物流感知项目依赖
# pip install -r requirements_logistics.txt

# 核心依赖
ultralytics>=8.0.0          # YOLOv8/10 训练和推理
opencv-python>=4.8.0         # 图像处理
numpy>=1.24.0                # 数值计算
torch>=2.0.0                 # PyTorch
torchvision>=0.15.0          # 视觉工具

# 分割模型 (Segment Anything)
# pip install git+https://github.com/facebookresearch/segment-anything.git
# 或使用 pip install segment-anything
sam2                          # SAM2 (可选)

# 追踪算法
# pip install supervision
supervision>=0.20.0          # 目标追踪、检测可视化

# 视频处理
av>=12.0.0                   # 视频编解码

# API服务
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# 工具库
Pillow>=10.0.0
tqdm>=4.65.0
scipy>=1.11.0                # 图像质量评估
